# Activated only via SPRING_PROFILES_ACTIVE=spatial-accident-import. Runs the one-shot Accident
# Raw Spatial Layer importer (com.season.semiproject.spatial.accident.AccidentImportRunner) and
# exits -- never starts a web server, so it never runs during normal startup.
spring.main.web-application-type=none
